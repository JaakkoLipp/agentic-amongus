# Server (Milestone 2)

Fastify + WebSocket host. Runs `MatchRunner` from `@deduction/runtime` in realtime mode, maps client messages to engine inputs for the human seat, and streams observations/events. See `docs/PROTOCOL.md`.
